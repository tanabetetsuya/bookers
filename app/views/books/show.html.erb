<%= flash[:notice] %>
<div class="text-title">
  <span>Title:</span>
  <span><%= @book.title %></span>
</div>

<div class="text-body">
  <span>Body:</span>
  <span><%= @book.body %></span>
</div>
<%= link_to 'edit', edit_book_path(@book.id) %>
